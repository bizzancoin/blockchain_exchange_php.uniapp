

# 基本环境
centos 7 安装宝塔

```plain
sed -i 's/mirrorlist/#mirrorlist/g' /etc/yum.repos.d/CentOS-*

sed -i 's|#baseurl=http://mirror.centos.org|baseurl=http://vault.centos.org|g' /etc/yum.repos.d/CentOS-*
```

```plain
yum install -y wget && wget -O install.sh http://v7.hostcli.com/install/install_6.0.sh && sh install.sh
```

| Centos7 | nginx | redis |
| --- | --- | --- |
| php7.3 | mysql5.7 | |


后台地址

https://域名/btx

用户名密码在数据库替换就行，后台地址可以在`\routes\manages.php`文件中更换。  


> + nginx php7.3 mysql5.7 安装php扩展 fileinfo opcache redis imagemagick imap 并且禁用所有函数；
> + 去掉验证码后端文件 `LoginController` 放在app\Http\Controllers\Api目录下
> + 代理商后台地址  https://域名/agent
> + 后端源码 **全局** 修改宝塔文件目录，修改成自己的`/www/wwwroot/domain`
> + 放行所有端口
> + 上传源码  设置public运行目录  laravel5伪静态   
>



```plain
yum install java -y
yum install python3
pip3 install websocket-client redis
cd /www/wwwroot/woo.worldbrcoin.xyz
#清理缓存
php artisan config:cache
```

# Es
```java
rpm --import https://artifacts.elastic.co/GPG-KEY-elasticsearch
vi /etc/yum.repos.d/elasticsearch.repo
```

```java
[elasticsearch-7.x]
name=Elasticsearch repository for 7.x packages
baseurl=https://artifacts.elastic.co/packages/7.x/yum
gpgcheck=1
gpgkey=https://artifacts.elastic.co/GPG-KEY-elasticsearch
enabled=1
autorefresh=1
type=rpm-md
```



```java
yum install elasticsearch -y
service elasticsearch start
#检查是否启动 curl -X GET localhost:9200
```

# 计划任务
```plain
#队列监听-每月一次
cd /www/wwwroot/woo.jys2026.xyz && php artisan queue:work

#执行计划-一分钟
cd /www/wwwroot/woo.jys2026.xyz && php artisan schedule:run

#webmsgsender -每月一次 
//源码部署后，手动执行
cd /www/wwwroot/woo.jys2026.xyz/public/vendor/webmsgsender && php start.php restart

#websocket client restart -每月一次
//源码部署后，手动执行
cd /www/wwwroot/woo.jys2026.xyz && php artisan websocket:client restart

#矿机返现执行脚本  每天晚上12点执行一次就行
https://woo.jys2026.xyz/api/user/kj
```

# nginx站点配置反向代理
```plain
location ~/(wss|socket.io) {        
        # 此处改为 socket.io 后端的 ip 和端口即可
        proxy_pass http://127.0.0.1:2000;
 
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_http_version 1.1;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
    }
```



